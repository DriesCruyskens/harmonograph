import Harmonograph from './Harmonograph.js'
import "sketch-info/sketch-info.js";

// Only executed our code once the DOM is ready.
window.onload = function() {
    const d = new Harmonograph('paper-canvas');
}